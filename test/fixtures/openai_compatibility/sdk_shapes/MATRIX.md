# OpenAI SDK Compatibility Decision Matrix

This matrix classifies the metadata-only SDK shape fixtures and the explicit
unsupported `/v1` endpoints probed by SDKs. It is a planning contract for Tasks
4-10, not a claim of broad OpenAI API coverage.

Decisions are limited to `accept`, `translate`, `reject`, or `passthrough`:

- `accept`: codex-pooler accepts the narrow shape as already compatible with its current OpenAI compatibility boundary
- `translate`: codex-pooler accepts the narrow SDK shape and rewrites it into a Codex-compatible upstream request shape
- `reject`: codex-pooler should return a deterministic OpenAI-shaped error before upstream dispatch
- `passthrough`: codex-pooler accepts the narrow shape and forwards the relevant field or item upstream without owning the resulting upstream state

| ID | SDK | Version | Endpoint | Scenario | Observed shape summary | Decision | Owner task | Code target | Test target | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `openai-node.responses.basic_text.v1` | OpenAI Node `openai` | `6.39.0` | `POST /v1/responses` | Basic Responses text creation with one synthetic user message | Top-level `model`, `input`, `stream`; one user message represented as one `input_text` part | `translate` | 6 | `CodexPooler.Gateway.OpenAICompatibility.Responses` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler_web/controllers/v1/responses_controller_test.exs` | Baseline Responses text shape stays narrow; Task 6 owns adjacent Responses field coverage while structured output, reasoning, and service tier cases are expanded |
| `openai-python.responses.executable_custom_tool.v1` | OpenAI Python `openai` | `2.52.0` | `POST /v1/responses` and `GET /v1/responses` websocket `response.create` | Executable Responses custom tool with an exact typed custom choice | Custom tool requires `type=custom` and nonblank `name`; optional description, boolean `defer_loading`, nullable direct/programmatic `allowed_callers`, and omitted/text/lark/regex format; typed choice has exact `type` and `name` | `accept` | issue 241 | `CodexPooler.Gateway.OpenAICompatibility.Responses` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler/gateway/payloads/payload_normalizer_test.exs`; `test/codex_pooler_web/controllers/v1/responses_controller_test.exs`; `test/codex_pooler_web/controllers/v1/responses_websocket_programmatic_test.exs`; `test/codex_pooler_web/controllers/runtime/compatibility_contract_test.exs` | Metadata-only provenance comes from local OpenAI Python 2.52.0 generated request types. The adapter accepts the SDK shape, Full mode preserves its typed choice, and Lite mode rejects that map-shaped choice before dispatch with `unsupported_parameter` for `tool_choice`. This executable definition is separate from custom replay support and from the official nested Chat wrapper. Actual execution availability depends on the selected model and upstream account; no broad OpenAI tool parity is claimed |
| `openai-responses.truncation.auto_disabled.v1` | OpenAI-compatible Responses shape | n/a | `POST /v1/responses` | Responses truncation control emitted by SDK clients | Top-level `truncation` is a string; only `auto` and `disabled` are accepted | `accept` | 15 | `CodexPooler.Gateway.OpenAICompatibility.Responses`; `CodexPooler.Gateway.OpenAICompatibility.Matrix` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler_web/controllers/v1/responses_controller_test.exs` | Accepted for public SDK compatibility but not forwarded upstream; unsupported values reject locally before dispatch |
| `openai-responses.reasoning_context.v1` | OpenAI-compatible Responses shape | n/a | `POST /v1/responses` | Responses reasoning context control emitted by SDK clients | Nested `reasoning.context` is a string; accepted values are `auto`, `current_turn`, and `all_turns` after trim/lowercase normalization | `accept` | reference update 2026-06-17 | `CodexPooler.Gateway.OpenAICompatibility.Responses` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler_web/controllers/v1/responses_controller_test.exs`; `test/codex_pooler_web/controllers/runtime/compatibility_contract_test.exs` | Accepted values are forwarded through the Responses adapter. Unknown, empty, and non-string values reject locally with `reasoning.context` before upstream dispatch |
| `openai-input-audio.bounded_five_formats.v1` | OpenAI-compatible Responses and Chat shapes | n/a | `POST /v1/responses`; `POST /v1/chat/completions` | Bounded `input_audio` request using the existing public `{data, format}` shape | Public labels are exactly `wav`, `mp3`, `m4a`, `webm`, and `ogg`; they canonicalize to `audio/wav`, `audio/mpeg`, `audio/mp4`, `audio/webm`, and `audio/ogg`; decoded input is limited to 52,428,800 bytes with a 69,905,068 non-whitespace encoded precheck | `translate` | Todo 5 | `CodexPooler.Gateway.OpenAICompatibility.Responses.Input.Audio`; `CodexPooler.Gateway.OpenAICompatibility.Responses`; `CodexPooler.Gateway.OpenAICompatibility.Chat` | `test/codex_pooler_web/controllers/v1/responses_controller_test.exs`; `test/codex_pooler_web/controllers/v1/chat_completions_controller_test.exs`; `test/codex_pooler_web/controllers/runtime/compatibility_contract_test.exs` | Accepted ASCII whitespace is normalized before canonical backend serialization; malformed, empty, unsupported, and oversized inputs fail as sanitized invalid requests before upstream dispatch and remain metadata-only outside the dispatch boundary |
| `openai-python.chat.basic_tool.v1` | OpenAI Python `openai` | `2.38.0` | `POST /v1/chat/completions` | Basic Chat Completions request with one synthetic user message and one nested function tool | Top-level `model`, `messages`, `tools`, `tool_choice`, `stream`; Chat tool uses nested `function` object with `parameters` | `translate` | 5 | `CodexPooler.Gateway.OpenAICompatibility.Chat`; `CodexPooler.Gateway.OpenAICompatibility.Responses` | `test/codex_pooler_web/controllers/v1/chat_completions_controller_test.exs`; `test/codex_pooler/gateway/openai_compatibility/core_test.exs` | Official SDK chat shape requires `messages`; chat nested function tools are translated through the Chat compatibility adapter |
| `openai-node.chat.custom_tool.v1` | OpenAI Node `openai` | commit `6fa9152eb97b7a36e3c555fbdeaaa241423ae91e` | `POST /v1/chat/completions` | Chat Completions custom definition, named choice, completed call, and split streamed input | Request tools use exact outer `type=custom` and nested `custom` with required nonblank `name` plus optional `description` and `format`; named choice nests the custom name; output calls use `type=custom` with nested `custom.name` and free-form `custom.input` | `translate` | issue 56 | `CodexPooler.Gateway.OpenAICompatibility.Chat`; `CodexPooler.Gateway.OpenAICompatibility.ChatCompletions` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler/gateway/openai_compatibility/chat_completions_test.exs`; `test/codex_pooler_web/controllers/v1/chat_completions_controller_test.exs`; `test/codex_pooler_web/controllers/runtime/compatibility_contract_test.exs` | The adapter flattens the official nested request definition and choice into the supported Responses custom subset, then restores the Chat custom-call shape for completed JSON and SSE. Split custom input remains free-form text and is never parsed as function JSON. Malformed wrappers and unrelated tool families reject locally; execution availability remains model/account dependent and no broad Chat tool parity is claimed |
| `openai-node.audio.transcription_repeated_lists.v1` | OpenAI Node `openai` | `7.3.0` | `POST /v1/audio/transcriptions` | Multipart transcription with a caller model label and repeated keyword/language lists | Required `file` and `model` parts; SDK serializes list entries as repeated `keywords[]` and `languages[]` parts in order, including duplicates | `translate` | 5 | `CodexPooler.Gateway.OpenAICompatibility.Audio`; `CodexPooler.Gateway.Payloads.TranscriptionPayload` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler/gateway/payloads/transcription_payload_test.exs`; `test/codex_pooler/gateway/payloads/payload_normalizer_test.exs` | Checked-in SDK source proves the multipart wire shape. Backend tests prove the narrow alias canonicalization and bounded list handling; this does not prove provider-level transcription semantics, model discovery, or broad Audio API parity |
| `openai-python.audio.transcription_repeated_lists.v1` | OpenAI Python `openai` | `2.52.0` | `POST /v1/audio/transcriptions` | Multipart transcription with a caller model label and repeated keyword/language lists | Required `file` and `model` parts; SDK serializes list entries as repeated `keywords[]` and `languages[]` parts in order, including duplicates | `translate` | 5 | `CodexPooler.Gateway.OpenAICompatibility.Audio`; `CodexPooler.Gateway.Payloads.TranscriptionPayload` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler/gateway/payloads/transcription_payload_test.exs`; `test/codex_pooler/gateway/payloads/payload_normalizer_test.exs` | Checked-in SDK source proves the multipart wire shape. Backend tests prove the narrow alias canonicalization and bounded list handling; this does not prove provider-level transcription semantics, model discovery, or broad Audio API parity |
| `local-chat.input_fallback.absent_or_empty_messages.v1` | Local compatibility fallback | n/a | `POST /v1/chat/completions`; `POST /backend-api/codex/v1/chat/completions` | Responses-shaped client payload sent to the chat route without usable `messages` | Top-level `input` is accepted only when `messages` is absent or `[]`; non-empty `messages` stays authoritative; omitted fallback `instructions` becomes `""` | `translate` | 5 | `CodexPooler.Gateway.OpenAICompatibility.Chat`; `CodexPooler.Gateway.OpenAICompatibility.Responses` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler_web/controllers/v1/chat_completions_controller_test.exs`; `test/codex_pooler_web/controllers/runtime/compatibility_contract_test.exs` | This is a codex-pooler local fallback only, not broad Responses-to-chat parity and not an official OpenAI SDK chat request shape |
| `vercel-ai.responses.tool_output_continuation.v1` | Vercel `ai` | `6.0.191` | `POST /v1/responses` | Responses tool-output continuation with stored response state flag and previous response reference | Top-level `previous_response_id`, `store`, `input`, `tools`; input item types include `item_reference`, `function_call_output`, and `message:user`; flat function tool shape | `passthrough` | 4 | `CodexPooler.Gateway.OpenAICompatibility.Responses`; `CodexPooler.Gateway.Payloads.ToolResultShape`; `CodexPooler.Gateway.Payloads.InputShape` | `test/codex_pooler/gateway/openai_compatibility/continuation_test.exs`; `test/codex_pooler/gateway/openai_compatibility/core_test.exs` | Current product boundary accepts the observed narrow continuation shape and forwards `previous_response_id`; fake previous ids may fail upstream with an OpenAI-shaped upstream status rather than local state ownership |
| `vercel-ai-sdk-openai.responses.flat_function_tool.v1` | Vercel OpenAI provider `@ai-sdk/openai` | `3.0.65` | `POST /v1/responses` | Provider-specific Responses request with flat function tool definition | Top-level `model`, `input`, `tools`, `tool_choice`; tool keys are flat `type`, `name`, `description`, `parameters` | `accept` | 5 | `CodexPooler.Gateway.OpenAICompatibility.Responses` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs` | Responses flat function tools are accepted only for the documented narrow function shape; malformed tool and `tool_choice` variants stay guarded |
| `vercel-ai-sdk-openai.responses.programmatic_tool_calling.v1` | Vercel AI SDK OpenAI provider `@ai-sdk/openai` | `3.0.65` | `POST /v1/responses` | Stateless Responses programmatic-tool calling | Top-level `model`, `store=false`, `input`, `tools`, `tool_choice`; closed `program`, `function_call`, `function_call_output`, and `program_output` vocabulary; exact type-only hosted tool and tool choice | `accept` | 8 | `CodexPooler.Gateway.OpenAICompatibility.Responses`; `CodexPooler.Gateway.OpenAICompatibility.Responses.Input`; `CodexPooler.Gateway.RequestCompression` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler/gateway/openai_compatibility/continuation_test.exs`; `test/codex_pooler_web/controllers/v1/responses_controller_test.exs`; `test/codex_pooler_web/controllers/v1/responses_websocket_programmatic_test.exs`; `test/codex_pooler_web/controllers/runtime/compatibility_contract_test.exs` | Closed-world support only: semantic tool-result continuations relay through JSON, SSE, and narrow Responses websocket while reference-only/ordinary continuations reject; Pooler forces upstream `stream=true` and `store=false`, excludes `program_output` from compression rewrites, and stores metadata only. Remote MCP and unrelated hosted tools remain rejected; no full OpenAI parity claim |
| `openai-responses.non_strict_function_schema_lowering.v1` | OpenAI-compatible Responses shape | n/a | `POST /v1/responses`; `POST /backend-api/codex/responses`; `GET /backend-api/codex/responses` websocket `response.create` | Non-strict function tool schema using SDK-permissive JSON Schema variants | Function tool `parameters` may contain boolean schema nodes, `const`, missing object or array type markers, missing object `properties`, missing array `items`, and unsupported descriptive JSON Schema keywords | `translate` | reference update 2026-06-17 | `CodexPooler.Gateway.Payloads.ToolSchemaLowering`; `CodexPooler.Gateway.OpenAICompatibility.Responses` | `test/codex_pooler/gateway/payloads/payload_normalizer_test.exs`; `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler_web/controllers/v1/responses_controller_test.exs`; `test/codex_pooler_web/controllers/runtime/compatibility_contract_test.exs` | Lowering is limited to non-strict function tools, including nested namespace function tools. Strict function tools and strict structured-output schemas stay strict and are not loosened |
| `vercel-ai-sdk-openai.responses.namespace_function_tool.v1` | Vercel OpenAI provider `@ai-sdk/openai` | `3.0.65+` | `POST /v1/responses` | Provider-specific Responses request with function tools grouped by `providerOptions.openai.namespace` | Top-level `tools` entry has `type=namespace`, nonblank `name`, nonblank `description`, and nested function tools with flat `type`, `name`, `description`, `parameters`, optional `strict`, and optional `defer_loading` | `accept` | namespace tool backport | `CodexPooler.Gateway.OpenAICompatibility.Responses`; `CodexPooler.Gateway.Payloads.StrictSchema` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler_web/controllers/v1/responses_controller_test.exs` | Accepted only for namespace entries containing nested function tools. Hosted tools, tool_search namespace markers, top-level deferred function tools, empty namespace labels, empty nested tool lists, malformed nested parameters, and non-boolean nested `strict`/`defer_loading` reject locally |
| `codex.responses.namespace_custom_tool.v1` | Codex native Responses shape | Codex commits `f21dc4638803f40046c9e294b0349782928f6b36` and `d4fb78bfc59009a2bbc3245d125bf8ba92a8e33e` | `POST /v1/responses` and `GET /v1/responses` websocket `response.create` | Codex Responses namespace containing an executable custom child and an exact typed custom choice | Top-level `tools` entry has exact `type=namespace`, nonblank `name` and `description`, and a nonempty `tools` list whose children are exact flat `function` or exact executable `custom` definitions; typed custom choice has exact `type` and `name` | `accept` | issue 17 | `CodexPooler.Gateway.OpenAICompatibility.Responses` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler_web/controllers/runtime/compatibility_contract_test.exs` | Direct public Responses HTTP and websocket preserve valid namespace custom definitions and exact typed custom choices in Full mode. HTTP, SSE, direct websocket, and owner-forwarded websocket output restore a missing or null custom_tool_call namespace only when one exact namespaced custom declaration matches, while explicit, flat, unknown, and non-unique namespaces remain unchanged. Lite keeps the existing pre-dispatch `unsupported_parameter` rejection for map-shaped `tool_choice`; hosted/MCP/tool_search/nested namespace children, blank namespace names, malformed custom fields, and global executable-name collisions remain excluded. Chat uses a separate official nested wrapper and does not add namespace support. This Codex provenance is separate from the OpenAI Python direct-custom, OpenAI Node Chat-custom, and Vercel namespace-function rows. |
| `openai-responses.additional_tools.request_input_item.v1` | OpenAI-compatible Responses shape | Node `openai` `6.41.0`; Python `openai` `2.40.0` | `POST /v1/responses`; `POST /v1/chat/completions` fallback input only | Request-shaped Responses `additional_tools` input item | Input item has `type=additional_tools`, `role=developer`, required `tools`, and optional nonblank string `id`; it stays inside `input` | `accept` | 5 | `CodexPooler.Gateway.OpenAICompatibility.Responses.Input` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler_web/controllers/runtime/compatibility_contract_test.exs` | Accepted only as request-shaped Responses input item. It is non-executable, not top-level, not merged into top-level `tools`, and not sufficient for `tool_choice`; nested remote MCP tool definitions remain rejected. Output-shaped `additional_tools` request input remains rejected |
| `opencode.responses.replay_continuation.v1` | OpenCode `opencode-ai` | `1.15.12` | `POST /v1/responses` and `GET /v1/responses` websocket `response.create` | Responses replay continuation with assistant output, reasoning summary, function call with optional `namespace`, and structured tool output item replay | Top-level `previous_response_id`, `store`, `input`, `tools`; input item types include `message:assistant`, `reasoning`, `function_call`, and `function_call_output`; replayed `function_call` may include `namespace`; `function_call_output` may include optional nullable, nonblank string `name` and `namespace` on `output` or legacy `result`; content part types include `output_text`, `summary_text`, `input_text`, and `input_image` | `translate` | opencode v1 replay fix | `CodexPooler.Gateway.OpenAICompatibility.Responses` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler/gateway/openai_compatibility/continuation_test.exs`; `test/codex_pooler_web/controllers/v1/responses_controller_test.exs` | Omitted or null metadata is accepted; blank or non-string values reject locally. Accepted metadata is forwarded unchanged over HTTP and the narrow websocket bridge; unsupported hosted-tool items and image `file_id` references remain rejected locally |
| `codex.responses.internal_turn_metadata_passthrough.v1` | Codex native Responses shape | Codex reference `4a82ecc3c9fcb8b9f21d5144c40a0dfdab54a02a` / `5b95745eae1b13be75d6c395a6baec8e7b7ad99b` | `POST /v1/responses` | Durable Responses replay items carry the turn id that introduced them | Replay items may include `internal_chat_message_metadata_passthrough.turn_id` independently from legacy item `metadata`; supported item types include `message`, `reasoning`, `function_call`, `function_call_output`, and translated chat-style assistant/tool replay | `passthrough` | reference update 2026-06-23 | `CodexPooler.Gateway.OpenAICompatibility.Responses.Input` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler/gateway/openai_compatibility/continuation_test.exs` | Preserves the passthrough map verbatim for upstream replay/resume compatibility and keeps values out of request-log metadata |
| `codex.responses.custom_tool_call_namespace_replay.v1` | Codex native Responses shape | Codex reference `328e95110ca6c89d58a8c40ef775a5eb61e3c485` | `POST /v1/responses` and `GET /v1/responses` websocket `response.create` | Namespaced custom tool call replay and output continuation | `custom_tool_call` has required `call_id`, `name`, `input`, optional `namespace`; matching `custom_tool_call_output` has required `call_id`, `output`, optional `name`; both may carry internal turn metadata | `passthrough` | reference update 2026-06-28 | `CodexPooler.Gateway.OpenAICompatibility.Responses.Input` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler_web/controllers/v1/responses_controller_test.exs` | Custom replay items are accepted only through narrow validators. This replay contract is separate from direct Responses executable custom tool admission |
| `openai-responses.strict_nested_missing_type_repair.v1` | OpenAI-compatible Responses shape | n/a | `POST /v1/responses` and `GET /v1/responses` websocket `response.create` | Strict flat function parameters with an already-typed object root and an untyped nested object or array node | Direct Responses may insert only a missing nested `object` or `array` type when strict structural evidence is unambiguous | `translate` | issue 241 | `CodexPooler.Gateway.Payloads.StrictSchema.Repair`; `CodexPooler.Gateway.OpenAICompatibility.Responses` | `test/codex_pooler/gateway/payloads/strict_schema_repair_test.exs`; `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler_web/controllers/v1/responses_controller_test.exs`; `test/codex_pooler_web/controllers/runtime/compatibility_contract_test.exs` | Repair is direct Responses-only and applies to top-level strict flat functions and strict flat function children of accepted namespaces. It does not repair a missing root type, explicit type values, refs, definitions, combinators or their descendants, structured outputs, Chat, the older nested `function` wrapper shape, or backend routes. Public Responses and Chat strict schemas reject malformed, duplicate, or unsupported explicit type vocabulary; strict function and structured-output schemas are never non-strict-lowered |
| `omp.responses.stateless_tool_replay_status.v1` | Oh My Pi `omp` | observed 2026-06-22 local request artifact | `POST /v1/responses` | Stateless Responses replay after an output-token-length stop with prior tool calls and a new follow-up input | Top-level `model`, `store=false`, `stream=true`, `input`; no `previous_response_id`; input item types include idless encrypted `reasoning`, `function_call` with `status=completed` or `status=incomplete`, `function_call_output`, and `message:user` | `translate` | OMP replay fix | `CodexPooler.Gateway.OpenAICompatibility.Responses.Input` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler/gateway/openai_compatibility/continuation_test.exs` | Drops stateless reasoning replay and removes known `function_call.status` replay metadata before validation. Raw prompts, tool output, encrypted reasoning, and call ids remain metadata-only in evidence |
| `omp.responses.gpt56_clean_first_turn.v1` | Oh My Pi `@oh-my-pi/pi-coding-agent` | `16.3.14` | `POST /v1/responses` | Clean GPT-5.6 first turn with max reasoning, automatic summary, encrypted reasoning inclusion, priority tier, and one flat function tool | Top-level `model`, `input`, `instructions`, `stream`, `store`, `max_output_tokens`, `prompt_cache_key`, `reasoning`, `include`, `service_tier`, `tools`; user input omits explicit `type=message`; reasoning uses `effort=max`, `summary=auto` | `translate` | GPT-5.6 Responses Lite fix | `CodexPooler.Gateway.OpenAICompatibility.Responses`; `CodexPooler.Gateway.Payloads.PayloadNormalizer` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler/gateway/payloads/payload_normalizer_test.exs`; `test/codex_pooler_web/controllers/v1/responses_controller_test.exs` | The public adapter normalizes the message item. Selected Responses Lite models then force `reasoning.context=all_turns` and `parallel_tool_calls=false`; the trusted internal Lite header remains model-metadata driven |
| `hermes.responses.assistant_tool_call_replay.v1` | Hermes Agent | observed 2026-06-08 local state shape | `POST /v1/responses` | Chat-style assistant tool-call replay followed by a role=tool terminal result | Top-level `previous_response_id`, `store`, `input`; assistant item has `role=assistant`, empty `content`, and `tool_calls` with nested `function.name`/`function.arguments`; tool item has `role=tool`, `tool_call_id`, and object `content.output` | `translate` | Hermes replay fix | `CodexPooler.Gateway.OpenAICompatibility.Responses.Input` | `test/codex_pooler/gateway/openai_compatibility/continuation_test.exs`; `test/codex_pooler_web/controllers/runtime/compatibility_contract_test.exs` | Translates only the observed narrow replay: assistant `tool_calls` become Responses `function_call` input items and role=tool object content uses the string `output` field. Raw terminal output remains metadata-only and unsupported tool-call variants still reject locally |
| `hermes.responses.reasoning_tool_replay.v1` | Hermes Agent | observed 2026-06-08 Hermes 0.16 state shape | `POST /v1/responses` | Encrypted reasoning replay followed by Hermes' empty assistant message, a Responses `function_call`, and a role=tool terminal result | Top-level `previous_response_id`, `store`, `input`; input sequence is `reasoning`, empty `role=assistant` `content`, `function_call`, then `role=tool` with `tool_call_id` and object `content.output` | `translate` | Hermes reasoning replay fix | `CodexPooler.Gateway.OpenAICompatibility.Responses.Input` | `test/codex_pooler/gateway/openai_compatibility/continuation_test.exs` | Preserves the reasoning replay, converts the empty assistant message to an assistant `message` with `output_text`, preserves the function call, and translates the role=tool object output to `function_call_output`. Encrypted reasoning, terminal output, previous response id, and call id remain metadata-only |
| `hermes.responses.completed_assistant_replay.v1` | Hermes Agent | observed 2026-06-09 Hermes 0.16 state shape | `POST /v1/responses` | Ordinary replay without `previous_response_id` that includes encrypted reasoning, a completed assistant message, and a follow-up user message | Top-level `model`, `store`, `input`, SDK controls; assistant replay item has `type=message`, `role=assistant`, `id`, `phase`, `status=completed`, and `output_text` content | `translate` | Hermes replay fix | `CodexPooler.Gateway.OpenAICompatibility.Responses.Input` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler/gateway/openai_compatibility/continuation_test.exs` | Accepts only safe assistant message status metadata values on replay items. Raw prompt text, encrypted content, and message ids remain metadata-only in tests and fixtures |
| `openclaw.responses.assistant_thinking_replay.v1` | OpenClaw | observed 2026-06-09 OpenClaw 2026.6.1 session state shape | `POST /v1/responses` | Ordinary replay without `previous_response_id` during a second `--session-key` turn where the prior assistant message contributes a reasoning replay item and assistant text replay | Top-level `model`, `store`, `input`; OpenClaw session state contains assistant `thinking` plus `text`, while the transport sends `reasoning` with `content: []` plus assistant `message` with `output_text` annotations | `translate` | OpenClaw replay fix | `CodexPooler.Gateway.OpenAICompatibility.Responses.Input` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler/gateway/openai_compatibility/continuation_test.exs` | Drops raw assistant `thinking` replay metadata and unsupported reasoning `content: []`. Assistant `output_text` preserves only exact ordered `url_citation` annotations; malformed or unsupported annotation shapes reject before dispatch. Raw prompts, thinking content, signatures, and transport bodies remain metadata-only |
| `vercel-ai.responses.url_citation_replay.v1` | Vercel AI SDK `ai` | Vercel AI SDK reference `1c1a9d18e713d40cf3c32ca3d2ad4c8adc2951c2` | `POST /v1/responses` and `GET /v1/responses` websocket `response.create` | Manual Open Responses assistant-history replay with cited output text | Assistant `message` content contains `output_text` with optional ordered `annotations`; each accepted citation has exactly `type`, `start_index`, `end_index`, `url`, and `title`, with `type=url_citation` | `translate` | Todo 4 | `CodexPooler.Gateway.OpenAICompatibility.Responses.Input` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler/gateway/openai_compatibility/continuation_test.exs`; `test/codex_pooler_web/controllers/v1/responses_websocket_programmatic_test.exs` | Preserves exact citation values, order, explicit empty lists, and omission. Every malformed or unsupported annotation rejects before dispatch; this is a closed schema, not generic annotation passthrough |
| `openai.responses.websocket_stream_id.v1` | OpenAI Responses WebSocket `openai` | Node `156a8db2e41a72e28ac0a103f91858db209811aa`; Python `d9029e3ada3c008b4631d78a425743445730892a` | `GET /v1/responses` websocket `response.create` only | Optional Open Responses WebSocket stream identifier | `stream_id` is a 1..256-byte string matching `^[A-Za-z0-9_.-]+$`; accepted creates conditionally echo it on attributable Open Responses server events | `accept` | Todo 4 | `CodexPooler.Gateway.Transports.Streaming.WebsocketCodec`; `CodexPoolerWeb.CodexResponsesSocket` | `test/codex_pooler/gateway/transports/websocket_codec_test.exs`; `test/codex_pooler/gateway/transports/public_responses_terminal_test.exs`; `test/codex_pooler_web/controllers/v1/responses_websocket_programmatic_test.exs`; `test/codex_pooler_web/controllers/v1/responses_websocket_bridge_test.exs` | Same IDs are FIFO. Different IDs are accepted and echoed, but cross-ID concurrency is unspecified because Pooler conservatively serializes public turns per connection. `previous_response_id` remains independent. The ID is stripped before upstream dispatch and stays transient: it is excluded from request options, persistence, accounting, logs, telemetry, metadata, REST, native backend WebSockets, Chat, compact, and batches |
| `vercel-ai-sdk-openai.responses.detailed_reasoning_summary_default.v1` | Vercel OpenAI provider `@ai-sdk/openai` | `4.0.0-beta.75` | `POST /v1/responses` | Provider default reasoning summary when reasoning effort is configured and reasoningSummary is not disabled | Top-level `reasoning` object contains `effort=high` and `summary=detailed` with ordinary text input placeholders | `accept` | reference update 2026-06-20 | `CodexPooler.Gateway.OpenAICompatibility.Responses` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs` | `reasoning.summary=detailed` stays in the explicit allowlist and is forwarded as part of the supported Responses reasoning object |
| `vercel-ai-sdk-openai.responses.web_search_domain_filters.v1` | Vercel OpenAI provider `@ai-sdk/openai` | Current source `3bc0d4f40df7a77af4b181bc97dc1c54843545ab` | `POST /v1/responses` | Current Vercel web search tool with optional allow/block domain filters | Tool entry has `type=web_search` and may contain `filters.allowed_domains`, `filters.blocked_domains`, or both; `external_web_access` is omitted unless separately configured | `passthrough` | Todo 3 | `CodexPooler.Gateway.OpenAICompatibility.Responses` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler_web/controllers/runtime/compatibility_contract_test.exs` | Pooler validates and forwards accepted values unchanged. Each supplied list independently requires 1..100 nonblank strings without leading-whitespace, case-insensitive HTTP(S) schemes. Availability and enforcement depend on the selected model/account; hosted Codex enforcement of `blocked_domains` is not locally proven, so this row claims neither universal enforcement nor full OpenAI hosted-tool parity |
| `codex.responses.web_search.indexed.v1` | Codex native Responses shape | Codex reference `3a2712ea141a5acb243689f803f26e48ff751771` | `POST /v1/responses` | Indexed hosted web search request generated by Codex native tool spec | Tool entry has `type=web_search`, boolean `external_web_access=true`, and optional boolean `index_gated_web_access=true`; cached search omits the indexed flag with `external_web_access=false` | `passthrough` | reference update 2026-06-20 | `CodexPooler.Gateway.OpenAICompatibility.Responses` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler_web/controllers/runtime/compatibility_contract_test.exs` | Pooler requires only `type`; both access flags are optional and boolean when supplied, while indexed access still requires explicit external access. `search_context_size` and `user_location` remain rejected locally; domain-filter support and its confidence limit are recorded in the dedicated Vercel row |
| `openai-responses.web_search_preview.type_only.v1` | OpenAI-compatible Responses shape | n/a | `POST /v1/responses` | Advanced hosted web search tool with no local options | Tool entry contains only `type=web_search_preview` | `passthrough` | 9 | `CodexPooler.Gateway.OpenAICompatibility.Responses` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs` | Exact type-only `web_search_preview` passthrough remains allowed. Optional access flags and bounded domain filters belong only to `web_search`; `search_context_size` and `user_location` remain rejected |
| `openai-responses.image_generation.exact_known.v1` | OpenAI-compatible Responses shape | n/a | `POST /v1/responses` | Advanced image_generation tool on Responses compatibility surface | Tool entry is either type-only passthrough or the exact `/v1/images/*` translated keys `type`, `model`, `size`, `quality`, optional `background`, optional `input_fidelity`, optional `output_format` | `passthrough` | 9 | `CodexPooler.Gateway.OpenAICompatibility.Responses`; `CodexPooler.Gateway.OpenAICompatibility.Images` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler_web/controllers/v1/images_controller_test.exs` | Task 9 does not broaden direct Responses image tool options beyond exact known shapes; media/image routes keep their existing exact translation contract |
| `openai-node.responses.hosted-shell-history.v1` | OpenAI Node Responses schema and stream types | [openai-node `6eda5b186b4aaff68cfa9f2cb9d8711620f8fc0c`](https://github.com/openai/openai-node/commit/6eda5b186b4aaff68cfa9f2cb9d8711620f8fc0c), [PR 2360](https://github.com/openai/openai-node/pull/2360) | `POST /v1/responses`; `GET /v1/responses` websocket | Hosted-shell history from a prior Responses stream | Input items are exact closed-key `shell_call` and `shell_call_output` shapes; relay includes command added/delta/done and output-content delta/done events | `accept` | issue 76 | `CodexPooler.Gateway.OpenAICompatibility.Responses.Input.HostedShell`; `CodexPooler.Gateway.OpenAICompatibility.Responses` | `test/codex_pooler/gateway/openai_compatibility/hosted_shell_test.exs`; `test/codex_pooler/compatibility_matrix_test.exs`; `test/codex_pooler_web/controllers/runtime/compatibility_contract_test.exs` | Pooler forwards accepted replay history without executing commands or admitting shell tool declarations, local-shell history, remote MCP, SDK command-index accumulation, call/output pairing, ordering enforcement, or broad hosted-tool parity. Command/output values stay transient and metadata-only; public relay keeps the five event types with only existing sequence and websocket stream-id normalization |
| `openai-responses.advanced_hosted_tools.unsupported.v1` | OpenAI-compatible Responses shape | n/a | `POST /v1/responses` | Unsupported hosted/advanced tool families including top-level `tools[type=mcp]`, nested `input[type=additional_tools].tools[type=mcp]`, file_search, code_interpreter, computer_use, shell, local shell, apply_patch, tool_search, and deferred variants | Tool entries use hosted tool `type` values, including remote MCP locators such as `server_url`, `connector_id`, and recent SDK `tunnel_id`, or deferred markers rather than flat function-tool or accepted namespace-function contracts | `reject` | 9 | `CodexPooler.Gateway.OpenAICompatibility.Responses`; `CodexPooler.Gateway.OpenAICompatibility.Responses.Input` | `test/codex_pooler/gateway/openai_compatibility/core_test.exs`; `test/codex_pooler_web/controllers/v1/responses_controller_test.exs`; `test/codex_pooler_web/controllers/v1/chat_completions_controller_test.exs` | Remote MCP and unknown hosted tool runtime semantics are not implemented. They reject locally before upstream dispatch with deterministic OpenAI-shaped `invalid_request` errors, not `unsupported_endpoint` |
| `unsupported.v1.responses.retrieve.v1` | SDK-probed OpenAI public route | n/a | `GET /v1/responses/:id` | Response retrieval endpoint exposed by SDKs but not implemented by codex-pooler | Path-only unsupported route; no request body is parsed after auth and compatibility gates | `reject` | 8 | `CodexPoolerWeb.V1.UnsupportedRoutes`; `CodexPoolerWeb.V1.UnsupportedController` | `test/codex_pooler_web/controllers/v1/route_auth_test.exs` | Expected status `404`; OpenAI-shaped error code `unsupported_endpoint`; codex-pooler does not own public `/v1` response retrieval state |
| `unsupported.v1.responses.cancel.v1` | SDK-probed OpenAI public route | n/a | `POST /v1/responses/:id/cancel` | Response cancellation endpoint exposed by SDKs but not implemented by codex-pooler | Path-only unsupported route; malformed JSON is not parsed after auth and compatibility gates | `reject` | 8 | `CodexPoolerWeb.V1.UnsupportedRoutes`; `CodexPoolerWeb.V1.UnsupportedController` | `test/codex_pooler_web/controllers/v1/route_auth_test.exs` | Expected status `404`; OpenAI-shaped error code `unsupported_endpoint`; cancellation is not implemented for public `/v1` response ids |
| `unsupported.v1.responses.delete.v1` | SDK-probed OpenAI public route | n/a | `DELETE /v1/responses/:id` | Response deletion endpoint exposed by SDKs but not implemented by codex-pooler | Path-only unsupported route; no upstream dispatch or gateway reservation | `reject` | 8 | `CodexPoolerWeb.V1.UnsupportedRoutes`; `CodexPoolerWeb.V1.UnsupportedController` | `test/codex_pooler_web/controllers/v1/route_auth_test.exs` | Expected status `404`; OpenAI-shaped error code `unsupported_endpoint`; deletion is not implemented for public `/v1` response ids |
| `unsupported.v1.images.variations.v1` | SDK-probed OpenAI public route | n/a | `POST /v1/images/variations` | Image variation endpoint exposed by SDKs but not implemented by codex-pooler | Unsupported multipart-capable route returns before multipart parsing | `reject` | 8 | `CodexPoolerWeb.V1.UnsupportedRoutes`; `CodexPoolerWeb.V1.UnsupportedController` | `test/codex_pooler_web/controllers/v1/route_auth_test.exs`; `test/codex_pooler_web/controllers/v1/images_controller_test.exs` | Expected status `404`; OpenAI-shaped error code `unsupported_endpoint`; supported image routes remain limited to generation and edit surfaces |
| `unsupported.v1.embeddings.v1` | SDK-probed OpenAI public route | n/a | `POST /v1/embeddings` | Embeddings endpoint exposed by SDKs but not implemented by codex-pooler | Unsupported JSON route preserves auth and compatibility gates before oversized body parsing | `reject` | 8 | `CodexPoolerWeb.V1.UnsupportedRoutes`; `CodexPoolerWeb.V1.UnsupportedController` | `test/codex_pooler_web/controllers/v1/route_auth_test.exs` | Expected status `404`; OpenAI-shaped error code `unsupported_endpoint`; no embedding model compatibility is implied |
| `unsupported.v1.batches.v1` | SDK-probed OpenAI public route | n/a | `POST /v1/batches` | Batch API endpoint exposed by SDKs but not implemented by codex-pooler | Unsupported JSON route preserves auth and compatibility gates before oversized body parsing | `reject` | 8 | `CodexPoolerWeb.V1.UnsupportedRoutes`; `CodexPoolerWeb.V1.UnsupportedController` | `test/codex_pooler_web/controllers/v1/route_auth_test.exs` | Expected status `404`; OpenAI-shaped error code `unsupported_endpoint`; batch lifecycle is outside the current product boundary |
| `unsupported.v1.moderations.v1` | SDK-probed OpenAI public route | n/a | `POST /v1/moderations` | Moderations endpoint exposed by SDKs but not implemented by codex-pooler | Unsupported JSON route returns deterministic OpenAI-shaped error after auth and compatibility gates | `reject` | 8 | `CodexPoolerWeb.V1.UnsupportedRoutes`; `CodexPoolerWeb.V1.UnsupportedController` | `test/codex_pooler_web/controllers/v1/route_auth_test.exs` | Expected status `404`; OpenAI-shaped error code `unsupported_endpoint`; moderation APIs are not implemented |
| `unsupported.v1.fine_tuning.jobs.v1` | SDK-probed OpenAI public route | n/a | `POST /v1/fine_tuning/jobs` | Fine-tuning job endpoint exposed by SDKs but not implemented by codex-pooler | Unsupported JSON route returns deterministic OpenAI-shaped error after auth and compatibility gates | `reject` | 8 | `CodexPoolerWeb.V1.UnsupportedRoutes`; `CodexPoolerWeb.V1.UnsupportedController` | `test/codex_pooler_web/controllers/v1/route_auth_test.exs` | Expected status `404`; OpenAI-shaped error code `unsupported_endpoint`; fine-tuning lifecycle is outside the current product boundary |
